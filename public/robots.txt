User-agent: *
Allow: /
Disallow: /dashboard
Disallow: /verify-email

# Legal and auth pages stay crawlable (so Google can see noindex) but are not
# listed in the sitemap. Sitemap URL is appended by CI (deploy.yml).
